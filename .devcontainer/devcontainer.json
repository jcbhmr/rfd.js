{
  "image": "mcr.microsoft.com/devcontainers/javascript-node",
  "features": {
    "ghcr.io/devcontainers/features/rust": {},
    "ghcr.io/devcontainers/features/desktop-lite": {}
  },
  "forwardPorts": [6080, 5901],
  "updateContentCommand": "export DEBIAN_FRONTEND=noninteractive && sudo apt-get update -y && sudo apt-get install -y --no-install-recommends libglib2.0-dev libatk1.0-dev libgtk-3-dev && sudo rm -rf /var/lib/apt/lists/*"
}
